import { HeroSection, AboutSection, ContactSection } from "../../containers";

const Home = () => {
  return (
    <div className="modern-wrapper">
      <HeroSection />
      <AboutSection />
      <ContactSection />
    </div>
  );
};

export default Home;
